#pragma once

#include "Globals.hpp"

/*
 * Accepts a file that contains all the parameters for the mining program
 * in key value format
 */
class Config {
	public:
	Config(const char* inpfile);
	};
